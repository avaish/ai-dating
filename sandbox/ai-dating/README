
`pip-compile requirements.in`


`flask db migrate -m "users table"`
`flask db upgrade`
`flask db downgrade`

`python -m pytest tests`


`docker compose build`

`docker compose up`

`docker compose down`

`localhost/healthz`
