To recreate requirements,`pip-compile requirements.in`

`pip install -r requirements.txt`

`flask db migrate -m "users table"`
`flask db upgrade`
`flask db downgrade`

`flask shell`
`flask --debug run --port=5001`

`localhost:8001/healthz`

`python -m pytest tests`

`gunicorn -b localhost:8001 -w 4 "src.app:create_app()"`

`docker run -dp 5001:5001 -w /app -v "$(pwd):/app" ai-dating sh -c "flask run --host 0.0.0.0"`

`docker compose build`

`docker compose up`

`docker compose down`
